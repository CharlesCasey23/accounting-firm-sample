import { Gelasio, Open_Sans, Raleway } from "@next/font/google";

export const headingRaleway = Raleway({
  subsets: ['latin'],
  weight: '700',
});

export const subHeadingRaleway = Raleway({
  subsets: ['latin'],
  weight: '500',
});

export const normalRaleway = Raleway({
  subsets: ['latin'],
  weight: '400',
});

export const gelasio = Gelasio({
  subsets: ['latin'],
  weight: '400',
});

export const openSans = Open_Sans({
  subsets: ['latin'],
  weight: '300',
})